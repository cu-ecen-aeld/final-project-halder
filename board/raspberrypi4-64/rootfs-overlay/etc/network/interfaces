auto lo
iface lo inet loopback

auto eth0
iface eth0 inet dhcp
  post-up sleep 5; ip route del 192.168.178.0/24 dev eth0 || true
  post-up sleep 5; ip route del default via 192.168.178.1 dev eth0 || true

auto wlan0
iface wlan0 inet dhcp
  wpa-conf /etc/wpa_supplicant.conf
