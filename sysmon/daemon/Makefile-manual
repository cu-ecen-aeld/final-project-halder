CC := $(abspath ../../buildroot/output/host/bin/aarch64-linux-gcc)

CFLAGS := -Wall -Wextra -std=c11 -D_POSIX_C_SOURCE=200809L -I../../sysmon

SRCS := sysmond.c 
TARGETS := $(SRCS:.c=)

.PHONY: all clean

all: $(TARGETS)

%.: %.c
	$(CC) $(CFLAGS) -o $@ $<

clean:
	rm -f $(TARGETS)
